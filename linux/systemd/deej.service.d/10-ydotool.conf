[Unit]
# Button commands shell out to ydotool, which needs the daemon up and needs to
# be told where its socket is - deej inherits this environment when it runs
# /bin/sh -c on a button command.
Wants=ydotoold.service
After=ydotoold.service

[Service]
Environment=YDOTOOL_SOCKET=%t/.ydotool_socket
